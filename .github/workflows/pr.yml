name: CI - PR

permissions:
  actions: read
  contents: read
  packages: read
  security-events: write

on:
  workflow_dispatch:
  pull_request:
    branches: [main, refactor/remote-service-migration-v2]
    types: [reopened, synchronize, opened]

jobs:
  build-and-test:
    uses: ./.github/workflows/pipeline.yml
    secrets: inherit
